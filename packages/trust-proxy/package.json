{
  "name": "@trust-layer/trust-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint ok'",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@fastify/rate-limit": "^10.2.0",
    "@fastify/static": "^8.1.0",
    "@trust-layer/audit-store": "*",
    "@trust-layer/grounding-engine": "*",
    "@trust-layer/guard-engine": "*",
    "@trust-layer/providers": "*",
    "@trust-layer/shared": "*",
    "fastify": "^5.3.0",
    "zod": "3.24.0"
  },
  "devDependencies": {
    "tsx": "^4.22.3",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
